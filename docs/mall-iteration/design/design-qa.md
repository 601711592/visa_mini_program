# 个人中心及子页面检查 · 2026-09-16

- source visual truth: assets/personal-center-reference.png
- 设计尺寸：750 × 1500；预览宽度 375 / 390 / 430 / 750，窄窗等比缩放。
- 本轮新增页面使用已确认设计语言，尚无单独确认的逐页图片参考。
- 状态：默认已登录，可切换游客态；模拟数据刷新后重置。

## 已验证

- 主页面及 subpages.js 的 Node 语法检查通过。
- tests/flows.cjs 使用临时 jsdom 26.1.0 运行通过，未增加项目依赖。
- DOM 回归覆盖：订单状态筛选、支付失败/取消/成功、退款审核状态、不同商品明细的出行人归属、积分收支筛选及规则、券适用范围筛选、商品跳转及嵌套返回、邀请待上线状态、地址新增/编辑/默认/删除、昵称转义、游客地图访问、协议未勾选拦截、分阶段登录及恢复原订单筛选/积分去使用、退出时地址清理。
- 本地 HTTP 预览返回 200。
- 通过 Chrome 原生 UI 实际打开主页面，点击“查看全部”进入订单列表，获取页面 AX 树与完整窗口截图（本次对话工具输出）。可见订单状态栏、商品条目、金额、取消/付款/详情按钮；未见该列表首屏文字重叠或卡片溢出。

## 视觉证据与限制

- implementation screenshot: 当前对话中“检查订单列表的实际布局与状态入口”的 Chrome 窗口截图；未保存成文件。
- 预览选择宽度：390 CSS px；原生截图含浏览器窗口，未取得纯内容标准尺寸捕获。
- full-view comparison evidence: 尚未完成同尺寸参考/实现并排比较。
- focused region comparison evidence: 尚未完成。
- 颜色：CSS 精确使用 #006F78 / #E8F3F4 / #C58B52。
- 字体：系统 PingFang SC 优先，复用主页面字号层级。
- 间距：750 设计单位、24 卡片圆角、20 模块间距、子页操作 88 高。
- 图像：本地 Lucide 图标；商品使用文档允许的占位图标；头像沿用参考资产。
- 文案：邀请奖励、公司地址坐标保留待确认，不虚构数据。
- 内置浏览器连接仍失败；使用 Chrome 完成上述查看后，电脑锁定，工具报告无法自动解锁。其余子页截图、320/430 宽视觉检查和浏览器控制台检查未完成。

## 已知范围

- HTML 设计及模拟交互，不是 uni-app、小程序真机、真实支付或授权实现。
- 邀请记录与分享、地图导航因业务配置缺失保持空态/不可用。
- 首页/分类/商品浏览沿用归档原型，购物流程产生的订单、积分和优惠券状态已同步个人中心（内存模拟）。
- 退款超半年起算点尚待业务确认，仅演示近期订单；不宣称覆盖正式退款政策。

final result: blocked

阻塞项：需解锁电脑才能继续浏览器视觉检查；不能以 DOM 检查替代完整视觉验收。

## 蓝色调更新

主页面、子页面及归档流程外层主题已改为 #245FA8 / #EDF3FB，正文及分隔色同步偏蓝灰。布局、DOM 和交互逻辑未改。主色与白色文字对比度已计算，DOM 回归重新通过。原截图不作为新版配色验收证据；视觉复核仍待完成。

## 客户指定主色更新

主色改为 #8AD0F9，前景 #164E70，对比度 5.29:1。主页面、子页面及归档外层导航配色同步；原生导航文字改为 black。回归检查通过，仍未完成新版视觉截图验收。

## 图标配色试验

个人中心及新子页图标统一近黑 #202124＋主色 #8AD0F9，保留深蓝文字。功能图标主体保持深色轮廓，浅蓝仅辅助点缀。未改变事件与页面结构；待客户查看视觉效果。

## 图标最终调整

按最新反馈取消双色试配，图标轮廓、局部笔画及选中图标填充统一 #000000。其他配色及交互不变。

## 图标与文本层级试配

图标改为单色 #245B7A，订单 54 设计单位、权益和服务 44；文字分层 #202631 / #495566 / #737D8C，减少功能名字重。仅 CSS 调整，未改变行为。待视觉确认。

## 剩余页面补齐与检查

用户已确认深蓝图标及文字层级方案。新增 flow.css、pages.html 和 tests/checkout.cjs，统一剩余商城页面并接通模拟结算与个人中心状态。

两组 DOM 回归均通过：个人中心原有交互，以及购物车→结算→分商品出行人→选券→提交→支付失败/取消/重试/成功→个人中心订单、积分、优惠券同步。另验证不适用品类优惠券被拦截。

浏览器已恢复可访问：在 Chrome 实际打开确认订单页面，取得 AX 树和窗口截图，核对商品卡、出行人入口、邮寄地址、优惠券、积分和金额明细的布局。当前截图没有首屏重叠；尚未完成全页面逐屏及多尺寸视觉验收。此前 blocked 状态仅为历史记录，当前交付状态为 HTML 可评审，完整视觉 QA 待完成。
